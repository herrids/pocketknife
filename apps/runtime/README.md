# apps/runtime

Placeholder for the new agent runtime. The previous agent (`agent/`, a Claude
Agent SDK planner+builder that submitted manifests to the Go backend over
`POST /deploy`) was removed as part of the monorepo re-architecture; this is
where its replacement will be built.
